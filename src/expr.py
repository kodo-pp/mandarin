# -*- coding: utf-8 -*-

import src.tokens as tokens

"""
class Node:
    def __init__(self, name, ls):
        self.name = name
        self.ls = ls

    def __repr__(self):
        return 'Node:{}({})'.format(self.name, ', '.join(map(repr, self.ls)))

class Pending(Node):
    def __init__(self, val):
        self.val = val

    def __repr__(self):
        return 'Pending({})'.format(repr(self.val))

class Symbol(Node):
    def __init__(self, name):
        self.name = name

    def __repr__(self):
        return 'Symbol({})'.format(self.name)
"""
