class Vector
    def new(x, y)
    end

    def native native_method(a, b, Int c)

    def __multiply__(k)
        return Vector(self.x * k, self.y * k)
    end

    var x
    var y
end


def main()
    print('Hello world!')
end
